#include <bits/stdc++.h>
using namespace std;

vector<vector<int>> dp;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    
    int n, k, w, v;
    cin >> n >> k;
    dp.resize()

    for (int i = 1; i <= m; i++) {

    }

}