#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);

    int n;
    string arr[n];
    int ans = 0;
    string s;

    for (int i = 0; n > i; i++) {
        int c;
        cin >> c;
        arr[i] = c;
    }

    cout << arr[0];
    
    return 0;
}